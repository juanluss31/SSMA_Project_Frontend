query login($username: String!, $password: String!){
    login(username: $username, password: $password){
        accessToken
        refreshToken
    }
}

query validateToken {
    validateToken {
        id
        username
        password
        firstname
        lastname
        counter
    }
}