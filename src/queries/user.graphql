mutation register(
	$username: String!
	$email: String!
	$password: String!
	$firstname: String!
	$lastname: String!
) {
	register(
		username: $username
		email: $email
		password: $password
		firstname: $firstname
		lastname: $lastname
	) {
		user {
			id
			username
			email
			firstname
			lastname
		}
		accessToken
	}
}

mutation login($username: String!, $password: String!) {
	login(username: $username, password: $password) {
		user {
			id
			username
			email
			firstname
			lastname
		}
		accessToken
	}
}

mutation logout {
	logout {
		username
	}
}

query findAll {
	findAll {
		id
		username
		email
		firstname
		lastname
	}
}
